$kind: http-request
method: GET
url: "{{gatewayBaseUrl}}/content/{{contentId}}"
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 2000
description: Get a specific content item by its UUID.
